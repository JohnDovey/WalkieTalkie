# WalkieTalkie

Agent notes: **[docs/CLAUDE.md](docs/CLAUDE.md)** (includes Go Dependabot / multi-module `go.mod` hygiene).

Cursor rules live under `.cursor/rules/` (committed). Local Claude session state under `.claude/` is gitignored.
