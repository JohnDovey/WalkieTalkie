# Go build artifacts (GOPATH/GOCACHE are redirected off-repo per
# tools/go-env.sh; this just covers stray local builds)
/server/wt-server
/server/server
*.exe
*.test

# macOS
.DS_Store
._*

# Claude Code local session/tool state (machine- and session-specific,
# never meant to be shared/committed)
/.claude/
